
name: Build and deploy webpage
run-name: ${{github.actor}} is testing
on: push
    # push:
    #     paths:
    #       - 'hugo/**' 
    #       - 'content/**'
jobs:
    Checkout:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout
          uses: action/checkout@v5
        - name: Debug
          run: |
            pwd
            ls -al
        
    Build:
        runs-on: ubuntu-latest
        container:
            image: docker.io/hugomods/hugo:base
            # build --cleanDestinationDir -s ./hugo --ignoreCache
        steps:
        - name: List env
          run: |
               env | sort
        - name: List files 
          run: |
            pwd
            ls -al

