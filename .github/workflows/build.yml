
name: Build and deploy webpage
run-name: Build and deploy webpage
on: push
    # push:
    #     paths:
    #       - 'hugo/**' 
    #       - 'content/**'
jobs:
    Build:
        runs-on: ubuntu-latest
        container:
            image: docker.io/hugomods/hugo:ci-0.152.2
        steps:
        - name: Checkout
          uses: actions/checkout@v5  
        - name: Generating page...
          run: |
               ls -al hugo
               hugo mod vendor --source ./hugo
               hugo build --cleanDestinationDir --source ./hugo --ignoreCache
               ls -al hugo/public 

